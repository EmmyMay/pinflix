const state = {
    tweetData: [],
    tikData: []
};

const getters = {
    tweetEmbeds: (state) => state.tweetData,
    tikEmbeds: (state) => state.tikData
};

const actions = {

};


const mutations = {

};

export default {
    state,
    getters,
    actions,
    mutations
};